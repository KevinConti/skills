# Handback — stop before commit *(inline)*

On approval, **stop**. Don't commit or push. Summarize what's ready and optionally draft a commit message. For **(b)/(c)**, surface the **contract ADR as a second deliverable** to the backend — offer to kick off `kev-dev-backend` for the endpoint, or hand the ADR to the backend team. For **(b)/(c) + block**, the handback is "UI + contract agreed; frontend implementation pending the backend endpoint" — resume at the implement step once it lands.
