# Resolve the work item *(inline)*

The argument is a backlog **id/path** or a **freeform description**. Locate the backlog markdown if it's an id/path (try the path as-given, then `backlog/`, `docs/`, `tasks/`, or an issue tracker); otherwise use the text. Restate the scope in a sentence or two and confirm you're on the right item. Light check, not a gate.
